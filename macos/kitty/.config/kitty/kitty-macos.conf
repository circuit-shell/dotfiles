# macOS Specific UI
hide_window_decorations titlebar-only
background_opacity 0.9
background_blur 10

# macOS Shell Logic (Optimized for Homebrew tmux)
shell zsh --login -c "if command -v tmux >/dev/null 2>&1; then tmux attach || tmux; else zsh; fi"

